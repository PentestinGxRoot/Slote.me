{% extends 'MediashareAdminBundle::base.html.twig' %}

{% block content_right %}
    <div class="col-lg-12">
        <h1 class="page-header">
            Modifier la newsletter
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="fa fa-paper-plane"></i> <a href="{{ path('admin_newsletter') }}">Gestion des newsletters</a>
            </li>
            <li class="active">
                <i class="fa fa-pencil-square"></i> Modification
            </li>
        </ol>
    </div>
    <div class="col-lg-12">
        {{ form_start(edit_form, {'attr': {'class':'form-horizontal' }}) }}
        <fieldset>
            <div class="form-group">
                {{ form_label(edit_form.object) }}
                <div class="col-lg-10">
                    {{ form_errors(edit_form.object) }}
                    {{ form_widget(edit_form.object) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(edit_form.addresser) }}
                <div class="col-lg-10">
                    {{ form_errors(edit_form.addresser) }}
                    {{ form_widget(edit_form.addresser) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(edit_form.message) }}
                <div class="col-lg-10">
                    {{ form_errors(edit_form.message) }}
                    {{ form_widget(edit_form.message) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(edit_form.newsletterGroup) }}
                <div class="col-lg-10">
                    {{ form_errors(edit_form.newsletterGroup) }}
                    {{ form_widget(edit_form.newsletterGroup) }}
                </div>
            </div>
        </fieldset>
        {{ form_end(edit_form) }}

        <a href="{{ path('admin_newsletter') }}" class="btn btn-primary"><i class="fa fa-arrow-circle-left"></i> Retour
            à la liste</a>
    </div>
{% endblock %}

