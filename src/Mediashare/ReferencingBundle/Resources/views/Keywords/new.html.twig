{% extends 'MediashareAdminBundle::base.html.twig' %}

{% block content_right %}
    <div class="col-lg-12">
        <h1 class="page-header">
            Ajouter un mot clé
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="fa fa-key"></i> <a href="{{ path('admin_keywords') }}">Keywords</a>
            </li>
            <li class="active">
                <i class="fa fa-plus-circle"></i> Ajouter
            </li>
        </ol>
    </div>
    <div class="col-lg-12">
        {{ form_start(form, {'attr': {'class':'form-horizontal' }}) }}
        <fieldset>
            <div class="form-group">
                {{ form_label(form.keywords) }}
                <div class="col-lg-10">
                    {{ form_errors(form.keywords) }}
                    {{ form_widget(form.keywords) }}
                </div>
            </div>

        </fieldset>
        {{ form_end(form) }}

        <a href="{{ path('admin_keywords') }}" class="btn btn-primary"><i class="fa fa-arrow-circle-left"></i>
            Retour &agrave; la
            liste</a>
    </div>
{% endblock %}
